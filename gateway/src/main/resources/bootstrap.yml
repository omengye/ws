spring:
  application:
    name: ws-gateway
  cloud:
    consul:
      enabled: true
      host: consul
      port: 8500

      config:
        enabled: true
        format: YAML
        data-key: application


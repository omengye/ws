spring:
  application:
    name: ws-gcs
  cloud:
    consul:
      enabled: true
      host: consul
      port: 8500

      config:
        enabled: true
        format: YAML
        data-key: application

      discovery:
        prefer-ip-address: true
